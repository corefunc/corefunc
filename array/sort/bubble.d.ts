/**
 * @name arraySortBubble
 * @param {Array.<T>} array
 * @param {function(T, T):number} [compare]
 * @returns {Array.<T>}
 */
export declare function arraySortBubble<T>(array: Array<T>, compare?: (alpha: T, beta: T) => number): Array<T>;
