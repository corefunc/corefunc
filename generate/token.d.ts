/**
 * @category Generate
 * @name generateToken
 * @description Generate random string from latin characters and numbers.
 * @param {Number} [size=16]
 * @returns {String}
 */
export declare function generateToken(size?: number): string;
