export declare function stringFindMatch(needle: string, haystack: string[]): {
    ratings: {
        target: string;
        rating: number;
    }[];
    bestMatch: string;
    bestMatchIndex: number;
};
