/**
 * @category RegEx Make
 * @name regexMakeEscaped
 * @description Escapes `RegExp` special characters
 * @param {string} [text=''] Text to escape
 * @returns {string} Escaped text
 * @since 0.1.12
 */
export declare function regexMakeEscaped(text: string): string;
